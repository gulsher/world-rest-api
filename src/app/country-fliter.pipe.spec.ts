import { CountryFliterPipe } from './country-fliter.pipe';

describe('CountryFliterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryFliterPipe();
    expect(pipe).toBeTruthy();
  });
});
